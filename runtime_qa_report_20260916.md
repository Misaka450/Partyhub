# PartyHub 运行时质量评估报告 —— 启动稳定性与缺陷分级

> **评估日期**：2026-09-16
> **测试版本**：本地 `main` 分支 @ `5ead732`，Node `server.js`，地址 `http://127.0.0.1:8080`
> **评估范围**：（1）启动、资源加载、完整对局稳定性（崩溃 / 卡死 / 性能衰减）；（2）全面缺陷排查并按类型分级 —— 功能错误、视觉瑕疵、音频问题、玩法一致性、跨平台兼容性
> **测试方法**：自动化套件（单元/契约测试、CDP 视觉看门狗、19 款游戏拟人真机测试）+ 真实浏览器手动多人联机测试（3 个标签页、2 名玩家），并注入 JS 错误 / 未捕获 Promise / 长任务（Long Task）/ rAF 帧率 / 堆内存监控
> **测试客户端**：Chromium 142 内核；移动端视口（约 1000×2000）由自动化拟人测试截图覆盖，桌面宽度（738px）由手动实测覆盖

---

## 一、总体结论

**游戏可以成功启动、资源全部正常加载，并能稳定支撑完整的多人对局全程。本次实测未发现 Critical（致命）或 High（高危）级运行时缺陷。** 19 款游戏均可正常启动、游玩、结算颁奖并返回大厅，全程无客户端崩溃、无服务端重启。发现的问题均为打磨/健壮性级别：一个影响 9 款游戏的持续性界面横幅瑕疵、一个浮点数倒计时显示问题，以及若干低危边界/输入健壮性问题。

### 核心结果一览

| 检查项 | 结果 |
|---|---|
| 服务端启动 | ✅ 启动日志干净，仅一行提示，无错误 |
| 资源交付（首页、CSS、11 个插件、voice、socket.io、qrcode） | ✅ 17/17 全部 HTTP 200 |
| 单元 / 契约测试 | ✅ **70/70 通过，0 失败** |
| CDP 视觉/体验看门狗（5 大维度） | ✅ **5/5 通过** |
| 19 款游戏拟人真机测试（真实按键、真实 DOM、多机器人） | ✅ **19/19 通过**（说明见第五节） |
| 全流程浏览器控制台错误 | ✅ **0 个**（3 个被监控的客户端） |
| 服务端运行时错误 | ✅ 仅 2 个，均由本次故意发送的畸形测试报文触发，且被守卫拦截 |
| 游戏中整页刷新 + 席位认领 | ✅ 席位、私密手牌、舞台、当前回合全部恢复 |
| 运行性能（3D 数方块画布，12 秒采样） | ✅ 约 52 fps（自动化标签页被限频）、p95 帧间隔 25ms、最大 42ms、1 个长任务（结算时 71ms）、堆内存 28MB |
| 聊天 XSS | ✅ 攻击载荷以转义文本原样显示，`onerror` 未执行 |
| 深色主题可读性 | ✅ 已验证（黑色画布 / 白色文字） |
| 音频合成 | ✅ Web Audio 路径无报错（实际出声效果无法用机器判定） |

### 缺陷统计（运行时实测确认）

| 严重级别 | 功能错误 | 视觉瑕疵 | 音频 | 玩法一致性 | 兼容性 | 合计 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| Medium（中） | 1 ✅已修复 | 2 ✅已修复 | 0 | 0 | 0 | **3（全部已修复）** |
| Low（低） | 2（行为备注，见 L5/L6） | 2 ✅已修复 | 0 | 0 | 1 ✅已修复 | **6（4 项已修复，2 项为待产品决策的行为备注）** |
| 待确认（仅观察到一次） | — | 1 | — | — | — | **1** |

> **修复进展（2026-09-17 更新）**：3 个 Medium + 4 个可操作的 Low（L1/L2/L3/L4）均已修复并实测通过（详见各条"修复落地与实测"）；每轮修复后 70/70 单元测试全绿、IDE 诊断零问题。剩余：L5（刷新是否自动重连）、L6（离线席位入 UNO 轮转，属代码审查记录的深层引擎改动）需先明确产品预期；U1 仅观察到一次、无法复现，暂不处理。

---

## 二、Medium（中危缺陷）

### M1（视觉）9 / 19 款游戏中，大厅提示横幅在对局开始后仍停留在屏幕上　✅ 已修复（2026-09-16）

**类型**：视觉瑕疵 / 残留 UI 状态
**位置**：共享元素 `#word-hint-box`；文案在 [game.js:1910](public/game.js#L1910) 设置

房主点击开始游戏后，大厅提示条

> 👑 你是房主：请在下方卡带中挑选游戏，点击【开始游戏】

（队员看到的是"⏳ 房主当前选择：【…】· 等待开局"）在整个对局期间一直停留在游戏画面顶部。该共享元素需要**各游戏插件自行覆盖**，结果 **10 个插件做了覆盖，9 个完全没处理**。

**运行时证据**：
- 手动实测：UNO（`UNO_PLAYING`，已出牌）和西蒙节拍（`SIMON_INPUT`，0/4）中确认 —— 游戏倒计时正在跑，但直接读取 DOM 仍是该大厅文案。
- 自动化真机截图确认出现在 **UNO、阿瓦隆、谁是卧底、西蒙节拍、拆弹轮盘、盲猜数量、轨道小火车、折纸打孔、影子猜物**（`tests/screens/human_playtest_*.png`）。
- 正常显示的 10 款：你画我猜、瞬间数羊、几A几B、词汇炸弹、切披萨、盲压挑战、3D 数方块、决战 24 点、颜色大陷阱、找零大师。

**影响**：仅影响观感 —— 残留的错误指令在手机布局顶部与游戏内容争抢注意力；无功能影响。
**修复建议**：在 `handleRoomState` 中当 `state.status !== 'LOBBY'` 时统一清空/隐藏该元素（默认隐藏，需要展示的插件再主动写入），不要依赖每个插件各自记得覆盖。
**修复落地与实测**：[game.js:1942-1953](public/game.js#L1942-L1953) 非大厅状态下按**内容识别**清理（仍是大厅引导文案或初始占位"等待房主开始..."时才清空）。没有采用"状态跃迁即清空"的方案——实测发现颜色大陷阱的 `stroop_new_question` 事件在服务端先于 room_state 广播（[stroopTrap.js:129](games/stroopTrap.js#L129) 先于 [:141](games/stroopTrap.js#L141)），跃迁清空会误删事件刚写入的提示；按内容匹配对事件顺序天然免疫。客户端资源版本号 v1.2.0 → v1.2.3（[index.html](public/index.html)）触发缓存刷新。实测四场景全过：大厅横幅正常 / UNO 对局提示为空 / 颜色大陷阱连击提示保留 / 24 点倒计时提示保留，控制台 0 错误。

### M2（视觉）词汇炸弹倒计时显示浮点数乱码：`6.800000000000001 s`　✅ 已修复（2026-09-16）

**类型**：视觉瑕疵 / 格式化问题
**位置**：[wordBomb.js:96](games/wordBomb.js#L96)（`room.wbTime` 赋值时未取整）；同时渲染在顶部计时器胶囊和提示条（`partyArcade.client.js:349`）

**运行时证据**：真机截图 `human_playtest_word-bomb.png` —— 引信燃烧时顶部胶囊显示 **6.800000000000001 s**，提示条显示"倒计时 6.8000000000000001s"。配置为非整数引信时长时，浮点连减累积 IEEE-754 误差所致。
**影响**：在最显眼的两个 UI 位置显得像坏了；纯属显示层问题，计时本身准确。
**修复建议**：初始化处加 `room.wbTime = Math.round(...)`（拆弹轮盘已有取整写法可参照）。
**修复落地与实测**：[wordBomb.js:95-97](games/wordBomb.js#L95-L97) 基础引信在 4~30 秒钳制之后整体 `Math.round`（惩罚值本就是整数 2.0，无其他浮点来源）。引擎级实测：7.5→8、6.8→7、8→8、3.2→钳制为 4、100→钳制为 30、未配置→默认 8，全部为整数。

### M3（功能 / 健壮性）畸形 Socket 报文直接抛进进程级守卫，发送方收不到任何响应　✅ 已修复（2026-09-16）

**类型**：功能缺陷 / 输入健壮性（同时是轻微的日志刷量风险）
**位置**：[server.js:304](server.js#L304)（`join_room`）、[server.js:695](server.js#L695)（`send_chat`），以及其他解构式处理器

**运行时证据**（QA 中故意发送，服务端控制台逐字捕获）：

```
⚠️ [未捕获异常] 已拦截，服务继续运行: TypeError: Cannot destructure property 'roomId' of 'object null' as it is null.
    at Socket.<anonymous> (server.js:305:29)
⚠️ [未捕获异常] 已拦截，服务继续运行: TypeError: text.trim is not a function
    at Socket.<anonymous> (server.js:695:33)
```

- `socket.emit('join_room', null)` → 同步抛异常 → 进程守卫保住了服务（这点是好的），但**永远不会回发 `join_error`**，发送该报文的客户端会在登录界面无限等待。
- `socket.emit('send_chat', { text: 12345 })` → `text.trim is not a function`，同一路径。
- 未认证连接可反复触发，刷爆服务端错误日志。

**影响**：不崩溃（守卫有效，验证 —— socket 保持连接、房间继续可玩），但会造成客户端无响应挂起以及错误日志无界增长。
**修复建议**：给报文加默认值（`(payload = {}) => { const {...} = payload || {}; }`），聊天增加 `typeof text === 'string'` 校验。
**修复落地与实测**：8 个"先解构后校验"的处理器（join_room / switch_game / send_chat / send_reaction / transfer_host / kick_player / voice_signal / voice_status）统一改为 `(payload) => { const {...} = payload || {}; }`（[server.js:305](server.js#L305) 起；注意 `= {}` 默认值挡不住 `null`，必须用 `payload || {}`，且 join_room 解构必须用 `let` 因函数体会重赋值 trim/兜底 token）。send_chat 增加 `typeof text !== 'string'` 拦截。实测：`join_room null` 现在正确回发 `join_error`（"房间号或昵称格式不正确"）；数字聊天被静默拒绝且不影响随后正常消息；其余 6 个 null 报文全部不再抛错，socket 保活，服务端日志 0 异常。

---

## 三、Low（低危缺陷与行为备注）

### L1（视觉 / 兼容性）UNO 手牌扇形在手机屏幕两侧被裁切　✅ 已修复（2026-09-17）
`human_playtest_uno.png`：最左（红色 1）和最右（黄色 4）两张牌紧贴视口边缘被切掉，两侧没有留白。牌仍可滑动/可点，但扇形看起来像被裁断。
**修复落地与实测**：[style.css:4040-4049](public/style.css#L4040-L4049) 三档断点（桌面/平板/手机）手牌容器改为左右对称留白（14/10/8px）并叠加 `env(safe-area-inset-left/right)` 刘海屏安全区，消除原先 14/4、8/2、6/2 的不对称裁切。实测 294px 视口下容器左右内边距均为 8px。

### L2（视觉 / 文案）你画我猜在选词前直接显示兜底文案"题目：选中词 (分类: 常用)"　✅ 已修复（2026-09-17）
画师尚未选词时，[drawGuess.client.js:346](public/games/drawGuess.client.js#L346) 用字面量"选中词"兜底并当作当前标题展示（`human_playtest_draw-guess.png`），读起来像一个真实但无意义的题目。
**修复落地与实测**：[drawGuess.client.js:342-352](public/games/drawGuess.client.js#L342-L352) 题目未就绪的短暂窗口统一显示"题目准备中…"，且无分类时不再硬拼"(分类: 常用)"。直接调用插件渲染实测：无题目状态 → 徽标/提示均为"题目准备中…"且不抛异常；有题目（长颈鹿/动物）→ 正常显示"题目：长颈鹿 (分类: 动物)"。

### L3（视觉 / 瞬时）拆弹轮盘的得分浮层与顶部状态栏重叠　✅ 已修复（2026-09-17）
`human_playtest_bomb-roulette.png`：浮动的"+50分"提示约 1 秒内渲染在顶栏/提示区（和炸弹图标）之上。仅瞬时重叠。
**修复落地与实测**：入房/被踢/退房三个屏幕切换点给 body 加/移除 `in-room` 类（[game.js:1190](public/game.js#L1190)、[:1255](public/game.js#L1255)、[:1779](public/game.js#L1779)），[style.css:317-321](public/style.css#L317-L321) 据此把全局 toast 在房间内下移到 92px（52px 导航栏 + 32px 副状态栏之下，叠加安全区）。实测：房内 toast 容器 top=92px；经真实"退出→确认退出"流程后类被移除、screen 回到登录页、toast 恢复 top=16px。

### L4（兼容性，潜伏问题 —— 本环境未复现）`ctx.roundRect()` 无 polyfill 兜底　✅ 已修复（2026-09-17）
[perfectSlice.client.js:458](public/games/perfectSlice.client.js#L458)、[:548](public/games/perfectSlice.client.js#L548) 调用了 `CanvasRenderingContext2D.roundRect()`（需 Chrome ≥99 / Safari ≥16 / Firefox ≥112）。QA 浏览器为 Chromium **142**，功能正常，因此该缺陷原本无法在实测中复现；但在旧版安卓 WebView / 应用内置浏览器（如微信内核）上会在每一帧动画抛错并导致画布空白。
**修复落地与实测**：新增模块内助手 `roundedRectPath`（[perfectSlice.client.js:40-58](public/games/perfectSlice.client.js#L40-L58)），原生 `roundRect` 存在则复用、不存在则用 `arcTo` 手绘等价路径并对半径做钳制；两处调用已替换。**用删除 `CanvasRenderingContext2D.prototype.roundRect` 的方式模拟旧内核**：开局切披萨进入 SLICE_CUTTING，画布 609px 宽、采样到 1915 个非透明像素（画面正常），控制台 0 错误。

### L5（行为备注）整页刷新后停在登录页，不会自动重连回房间
房间号没有持久化（只存了昵称/token/secret）。对局中按 F5 后用户看到登录页；重新输入**相同房间号**可正确认领回席位。已验证：重入后席位、私密手牌、舞台、当前回合全部恢复，零报错。刷新是否应自动重连属于产品决策，此处记录实际行为以免误判为 bug。

### L6（行为备注）掉线 90 秒宽限期内的离线席位会短暂参与 UNO 回合轮转
当断线玩家标签页在宽限期内重连/离开时，对方的回合提示曾短暂指向离线席位（状态在数秒内自愈；服务端 30 秒计时器会自动摸牌并跳过）。未观察到死锁，但移动弱网玩家可能看到令人困惑的"等待离线玩家"瞬间。与 2026-09-16 代码审查中记录的引擎层问题相关（UNO 在活跃玩家少于 2 人时继续轮转 / 中途加入者入轮转）。

### U1（待确认）谁是卧底的发言者一度显示为占位名"玩家名"
单张自动化截图（`human_playtest_undercover.png`）显示"当前发言 玩家名 等待进入下一阶段"，而真实机器人名字是"小马爱丽丝"等。手动实测无法复现，疑为客户端渲染前的帧间窗口被截到。仅记录供后续观察，暂不归类为已确认缺陷。

---

## 四、已验证正常的方面（稳定性证据）

- **大厅启动**：冷加载零控制台错误、零网络失败（网络记录里唯一的 `/@vite/client` 404 是自动化浏览器自身注入的 —— 代码库中无任何引用；对外部 Umami 的请求是已配置的统计脚本）。
- **插件架构**：13 个客户端模块（`window.PartyGames`，覆盖 19 款游戏）全部挂载成功；socket 正常连接；语音管理器初始化；ICE/TURN 配置正常下发。
- **UNO 完整真机对局**（2 名玩家，约 10 分钟）：出牌经服务端校验、回合轮转、30 秒超时自动摸牌/跳过、私密手牌私发、对局中房主点"返回大厅"（带确认弹窗）后**双方**客户端干净拆除、大厅完整重置。
- **断线重连**：刷新 → 重入房间 → 同一席位被认领，私密手牌（13 张）补发、游戏舞台与当前回合恢复；零控制台错误。
- **游戏生命周期**：3D 数方块完整跑完观察 → 抢答 → 结算；GAME_OVER 弹窗渲染出完整颁奖台（🥇/🥈、分数），再来一局 / 换个游戏 / 返回大厅三个按钮均可用；从数方块切到西蒙节拍后无错误、无残留界面。
- **性能**：数方块期间 12 秒 rAF 采样共 622 帧（标签页被限频下约 52 fps），平均帧间隔 19.3ms，p95 25ms，最大 41.7ms，仅在回合结算时出现 1 个 71ms 长任务，JS 堆 28MB。整局 UNO（每秒广播，约 10 分钟）两个客户端**零长任务**。
- **XSS**：聊天载荷 `<img src=x onerror=...><b>` 以转义文本（`&lt;img...&gt;`）渲染，未创建任何 `img`/`b` 节点，攻击标记未触发。
- **深色主题**：切换设置 `data-theme="dark"`；html 画布变黑、标题/正文为白/浅色 —— 可读，无对比度回退。
- **音频**：用户激活后调用 Web Audio `playSound('correct')` 不抛异常；各游戏音效路径无控制台错误。
- **服务端韧性**：经历完整压力工作负载（畸形报文、重复入房、对局中断线/刷新、反复拆除）后进程从未重启，日志中除启动行外只有第三节 M3 的 2 个被守卫拦截的异常。

---

## 五、环境 / 覆盖度说明（如实披露局限）

1. 拟人真机测试的外壳返回了退出码 1，但 **19 款游戏全部打印 PASS 且脚本自身退出码为 0**（`===HUMAN_EXIT=0===`）。非零退出码来自 TRAE 沙箱在测试结束**之后**清理浏览器进程时，捆绑的 Edge 触碰系统配置文件产生的告警 —— 不是产品缺陷。
2. **设备矩阵**：仅覆盖移动端模拟 Chromium（拟人测试）和桌面宽度真机浏览器。未在真机 iOS Safari / Android Chrome / 微信内置浏览器上执行；L4（roundRect）是该方向的主要风险点。
3. **音频可听性**：代码路径无错误，但无头自动化无法断言扬声器是否真的出声。
4. **多人语音（WebRTC）**：信令/ICE 配置路径加载干净；未在两台设备间实测 P2P 通话音频（需要麦克风授权/设备配合）。信令层已知问题记录在 2026-09-16 的代码审查报告中。

---

## 六、建议修复顺序

1. ~~**M3** —— 服务端报文默认值 + 聊天类型守卫~~ ✅ 已修复（2026-09-16）
2. ~~**M1** —— 集中处理 `#word-hint-box`~~ ✅ 已修复（2026-09-16）
3. ~~**M2** —— 引信时长加 `Math.round`~~ ✅ 已修复（2026-09-16）
4. ~~**L4** —— 在旧设备用户报出"切披萨白屏"之前补上 `roundRect` 降级~~ ✅ 已修复（2026-09-17）
5. ~~L1–L3~~ ✅ 已修复（2026-09-17）。**剩余待决策**：L5（刷新自动重连）、L6（UNO 离线席位轮转）需产品先定预期行为；U1 卧底占位名无法复现，保持观察。
