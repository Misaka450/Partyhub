services:
  draw-guess:
    build: .
    container_name: draw-guess-game
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./:/app
